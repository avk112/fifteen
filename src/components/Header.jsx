import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <h1 className="header__title">Fifteen Game</h1>
        </header>
    );
};

export default Header;